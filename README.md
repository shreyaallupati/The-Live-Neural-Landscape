# The-Live-Neural-Landscape
A shared canvas where multiple users click to "label" points (e.g., Green vs. Red). A PyTorch Neural Network lives on the server, learns from every click in real-time, and broadcasts the updated model performance (loss/accuracy) to everyone’s dashboard simultaneously.
